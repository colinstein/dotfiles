[user]
  name = Colin Stein
  email = colinstein@me.com
[core]
  editor = vim
  pager = less
  excludefiles = /Users/colin/.gitignore
[push]
  default = simple
[color "branch"]
  current = yellow
  local = green
  remote = cyan
[color "diff"]
  meta = blue
  frag = magenta bold
  old = red
  new = cyan
  whitespace = red reverse
[color "status"]
  added = cyan
  changed = yellow
  untracked = red
[diff]
  tool = vimdiff
[difftool]
  prompt = false
[github]
  name = colinstein
  token = TODO: Insert Token Here.
[alias]
  branches = branch -a
  unstage = reset -q HEAD --
  uncommit = reset --mixed HEAD~
  discard = checkout --
  unfuck = !git reset --hard HEAD && git clean -d -f
  graph = log --graph -20 --branches --remotes --tags  --format=format:'%Cgreen%h %Creset• %<(75,trunc)%s (%cN, %cr) %Cred%d' --date-order
  
